# แผนการรีเซ็ตฐานข้อมูลและอัปเดตแผนการวิ่งรายปีจาก Excel (2026-02-23)

## 🎯 เป้าหมาย
1. รีเซ็ตฐานข้อมูลเพื่อให้แอปเริ่มต้นจากสภาวะสะอาด (Clean State)
2. เปลี่ยนระบบแผนการวิ่งรายปีจากการคำนวณอัตโนมัติ เป็นการดึงข้อมูลจากไฟล์ `zone2_full.xlsx` ที่ผู้ใช้กำหนด
3. ปรับปรุง `ai-agent` ให้บันทึกความคืบหน้านี้

## 📋 รายการดำเนินการ
- [ ] ลบไฟล์ `longving.db` ใน Application Support
- [ ] ติดตั้ง `xlsx` และสกัดข้อมูลจาก `zone2_full.xlsx`
- [ ] สร้างไฟล์ `src/lib/yearlyPlanData.ts` เพื่อเก็บข้อมูลแผนถาวร
- [ ] แก้ไข `src/lib/planGenerator.ts` ให้ใช้ข้อมูลจากไฟล์ที่สร้างใหม่
- [ ] ยืนยันข้อมูลในหน้า UI ของแอป
